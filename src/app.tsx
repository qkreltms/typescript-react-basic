import * as React from 'react'
import { Header, About } from './component'

export default () => {
    return (
        <div className='container-fluid'>
            <Header/>
            <About/>
        </div>
    )
}